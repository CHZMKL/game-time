const chai = require('chai');
const assert = chai.assert;
const Paddle = require('../lib/brick.js');

describe('Brick', function(){

  context('has default size and position', function(){
    var brick = new Brick
  });
});
